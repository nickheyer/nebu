listen: 127.0.0.1:8484

# data_dir: /path/to/nebu
# cache_dir: /path/to/cache/nebu
# store_dir: /path/to/nebu/store

logging:
  level: info
  format: text

# Discord bots keep their tokens as rows; this names only the ffmpeg that samples video attachments, found on PATH when unset
# discord:
#   ffmpeg: /usr/bin/ffmpeg
